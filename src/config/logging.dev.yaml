# Development Logging Configuration
# All categories enabled by default for debugging

# Set to 'all' to enable all categories, 'none' to disable all
mode: categories

# Or specify individual categories (only used if mode is not 'all' or 'none')
categories:
  - ui
  - assistant
  - kpi
